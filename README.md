# Dockerized Go Application 🚀

مشروع بسيط بلغة Go تم تحويله إلى Docker image.

## طريقة التشغيل محليًا 🖥️

```bash
docker build -t go-docker-app .
docker run -p 8080:8080 go-docker-app
```

افتح المتصفح على:
```
http://localhost:8080
```

## رابط Docker Hub 🐳
(ضع هنا الرابط بعد رفع الصورة)
